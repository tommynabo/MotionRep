# This file is intentionally empty.
# Python dependencies are only for local development (see requirements-dev.txt).
# The backend runs on Node.js in Vercel. Video processing happens asynchronously.

